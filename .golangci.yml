linters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - govet
    - misspell
    - nolintlint

linters-settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/containernetworking/cni)
